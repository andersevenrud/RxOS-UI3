{
  "name": "@outernet/skylark-wikipedia-application",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "eslint": "eslint *.js",
    "build": "webpack",
    "watch": "webpack --watch",
    "stylelint": "stylelint index.scss src/**/*.scss",
    "prepublishOnly": "npm run eslint && npm run stylelint && rm ./dist/* && npm run build"
  },
  "dependencies": {},
  "devDependencies": {},
  "osjs": {
    "type": "package"
  }
}
